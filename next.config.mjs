/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  experimental: {
    // (keep space for future experimental flags if needed)
  }
};

export default nextConfig;
